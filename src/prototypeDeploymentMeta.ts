/** Static identity for this build — see PROTOTYPES.md at repo root. */
export const PROTOTYPE_DEPLOYMENT_META = {
  channelLabel: "MVP prototype",
  purposeLine:
    "M0 read-only schema viewer + M1 schema management — for PMs, solution review, and milestone alignment.",
} as const;
